import * as React from 'react';

// Styles
import * as styles from './separator.module.scss';

type SeparatorType = {};

export const Separator = ({}: SeparatorType) => {
  return <div className={styles.Separator}></div>;
};
