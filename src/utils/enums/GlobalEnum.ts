/**
 * The global enum
 */
export enum GlobalEnum {
  APP_DATA_FOLDER_NAME = 'Spectrum',

  // Database enums
  DB_FOLDER_PATH = 'Database',
  DB_FILE_NAME = 'spectrum.db',

  // Global settings enums
  SETTINGS_FOLDER = 'Settings',
  GLOBAL_STATE_FOLDER = 'Global',
  DEVICE_STATE_FILE = 'devices.json',
}
