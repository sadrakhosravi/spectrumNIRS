import * as React from 'react';

import styles from './seriesUnit.module.scss';

export const SeriesUnit = () => {
  return (
    <div className={styles.SeriesUnit}>
      <span />
    </div>
  );
};
